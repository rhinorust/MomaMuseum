<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MomaMuseum.MasterPage"
             Padding="10,10,0,0"
			       Icon="menu_icon.png"
			       Title="Master"
             BackgroundColor="#001533">
  <ContentPage.Content>
    <StackLayout VerticalOptions="FillAndExpand">
      <ListView x:Name="listView" VerticalOptions="FillAndExpand">
        <ListView.ItemTemplate>
          <DataTemplate>
            <ViewCell>
              <StackLayout Orientation="Horizontal" Spacing="25" HorizontalOptions="FillAndExpand">
                <Image Source="{Binding IconSource}"  HeightRequest="100"/>
                <Label Font = "22" Text="{Binding Title}" TextColor="White" VerticalOptions="Center" />
              </StackLayout>
            </ViewCell>
          </DataTemplate>
        </ListView.ItemTemplate>
      </ListView>
    </StackLayout>
  </ContentPage.Content>
</ContentPage>